<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.concesionaria">

    <application
        android:name=".aplication.UserAplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Concesionaria"
        tools:targetApi="31">
        <activity
            android:name=".ui.details.presenter.DetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.enviroment.presenter.EnvironmentsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.presenter.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.presenter.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".aplication.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
